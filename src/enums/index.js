'use strict';

/**
 * Module dependencies.
 */

const ALGORITHMS = require('./algorithms-enum');
const ENCODINGS = require('./encodings-enum');

/**
 * Export named enums.
 */

module.exports = {
  ALGORITHMS,
  ENCODINGS
};
